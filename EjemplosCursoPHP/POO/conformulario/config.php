<?php
// Definimos las constantes que se utilizarán para la conexión a la base de datos

// Definimos el host de la base de datos (por lo general 'localhost' cuando la base de datos está en el mismo servidor)
define('DB_HOST', 'localhost');

// Definimos el nombre de usuario que se usará para conectarse a la base de datos (en este caso es 'root')
define('DB_USUARIO', 'root');

// Definimos la contraseña para el usuario de la base de datos (en este caso está vacía)
define('DB_CONTRA', '');

// Definimos el nombre de la base de datos a la que queremos conectarnos ('pruebas')
define('DB_NOMBRE', 'pruebas');

// Definimos el conjunto de caracteres que queremos utilizar para la conexión (se usa 'utf8' para manejar correctamente caracteres especiales)
define('DB_CHARSET', 'utf8');
?>